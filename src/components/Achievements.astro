<section class="relative bg-green-900 text-white py-16 sm:py-20 px-6 sm:px-12 lg:px-20">
  <!-- Background Overlay -->
  <div class="absolute inset-0 opacity-10 bg-[url('/leaf-pattern.png')] bg-cover"></div>

  <!-- Content Row -->
  <div class="relative max-w-6xl mx-auto grid grid-cols-1 md:grid-cols-2 gap-12 items-center">
    
    <!-- Left Text -->
    <div class="text-center md:text-left">
      <p class="text-yellow-400 text-xs sm:text-sm font-semibold uppercase tracking-widest">
        Achievements
      </p>
      <h2
        class="mt-3 sm:mt-4 text-3xl sm:text-4xl md:text-5xl font-bold"
        style="font-family: 'Poppins'"
      >
        Let's Get Connect
      </h2>
    </div>

    <!-- Stats Grid -->
    <div class="grid grid-cols-1 sm:grid-cols-2 gap-8 text-center md:text-right">
      <!-- Item 1 -->
      <div>
        <p class="text-3xl sm:text-4xl font-bold text-center" style="font-family: 'Poppins'">24</p>
        <p class="mt-1 sm:mt-2 text-base sm:text-lg text-gray-200 text-center">Years Of Experience</p>
      </div>
      <!-- Item 2 -->
      <div>
        <p class="text-3xl sm:text-4xl font-bold text-center" style="font-family: 'Poppins'">10K</p>
        <p class="mt-1 sm:mt-2 text-base sm:text-lg text-gray-200 text-center">Happy Clients</p>
      </div>
    </div>

  </div>
</section>
